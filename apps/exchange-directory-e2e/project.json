{
  "root": "apps/exchange-directory-e2e",
  "sourceRoot": "apps/exchange-directory-e2e/src",
  "projectType": "application",
  "targets": {
    "e2e": {
      "executor": "@nrwl/cypress:cypress",
      "options": {
        "cypressConfig": "apps/exchange-directory-e2e/cypress.json",
        "devServerTarget": "exchange-directory:serve"
      },
      "configurations": {
        "production": {
          "devServerTarget": "exchange-directory:serve:production"
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/exchange-directory-e2e/**/*.{js,ts}"]
      }
    }
  },
  "tags": [],
  "implicitDependencies": ["exchange-directory"]
}
